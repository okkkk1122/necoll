# ═══════════════════════════════════════════════════════════════
# Necoll — Development Environment
# cp .env.example .env
# docker compose -f docker-compose.yml --env-file .env up --build -d
# ═══════════════════════════════════════════════════════════════

NODE_ENV=production
SITE_PORT=3011
# جلوگیری از لود خودکار docker-compose.override.yml در Development
COMPOSE_FILE=docker-compose.yml

# ─── دیتابیس PostgreSQL ──────────────────────────────────────
DB_USER=necoll
DB_PASSWORD=Aa@545544
DB_NAME=necoll_db
DATABASE_URL=postgresql://necoll:Aa%40545544@postgres:5432/necoll_db

# ─── آدرس عمومی (Development — Docker Desktop) ───────────────
SITE_URL=http://localhost:3011
CORS_ORIGIN=http://localhost:3011
NEXT_PUBLIC_SITE_URL=http://localhost:3011
NEXT_PUBLIC_API_URL=http://localhost:3011/api

# ─── داخلی ───────────────────────────────────────────────────
HOST=0.0.0.0
BACKEND_PORT=4000
API_INTERNAL_URL=http://backend:4000/api
REDIS_URL=redis://redis:6379
JWT_SECRET=necoll_jwt_super_secret_change_in_production

# ─── MinIO ───────────────────────────────────────────────────
MINIO_ROOT_USER=necoll_minio
MINIO_ROOT_PASSWORD=necoll_minio_secret
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_BUCKET=necoll-uploads
MINIO_PUBLIC_URL=http://localhost:3011/uploads

# ─── توسعه با hot-reload (اختیاری) ───────────────────────────
# COMPOSE_FILE=docker-compose.yml:docker-compose.dev.yml
# NODE_ENV=development

# ─── Production: از .env.production.example استفاده کنید ─────
# scripts/deploy.ps1 یا scripts/deploy.sh
